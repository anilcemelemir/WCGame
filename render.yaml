services:
  - type: web
    name: wcgame-lobby
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm run start:server
    healthCheckPath: /healthz
    envVars:
      - key: NODE_ENV
        value: production
